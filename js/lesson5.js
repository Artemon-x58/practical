// 3) УМОВА
// Зробити фіксований скрол на сторінці для менюшки, тобто,
//     щоб наше меню фіксувалось зверху браузера при скролі,
//     для того потрібно порахувати висоту хедера,
//         відстежуємо позицію скролу та
// додавати клас fixed - nav якщо скрол більше висоту
// хедера, в іншому випадку його видаляємо


/* const nav = document.getElementById('navigation');
const navHeader = document.querySelector('header');



// pageYOffset

// scrollY

    
const dataWidth = navHeader.clientWidth;
const dataHeight = navHeader.clientHeight;


function scrollControl() {

    if (scrollY > dataHeight) {
    nav.classList.add("fixed-nav")
    }
    else { 
    nav.classList.remove("fixed-nav")
    }
    
}

window.addEventListener('scroll', scrollControl)

 */










